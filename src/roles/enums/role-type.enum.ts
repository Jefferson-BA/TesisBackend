export enum RoleType {
  SUPERADMIN = 'superadmin', // Tú (acceso total al sistema)
  ADMIN = 'admin',           // Dueño/Gestor del restaurante
  USER = 'user',             // Cliente final/Comensal
}